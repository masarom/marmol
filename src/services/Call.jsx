const Call = () => {
  const cleanedData = JSON.stringify();
  /* const cleanedData = result.map((eachWord, i) => {
    return {
      index: i,
      name: eachWord.name,
      desc: eachWord.description,
    };
  }); */
  return cleanedData;
};

export default Call;
